{
  "name": "canonical-home-test",
  "version": "1.0.0",
  "description": "Take home test from Canonical",
  "main": "index.js",
  "scripts": {
    "build": "sass --load-path=node_modules styles.scss css/styles.css --style=compressed",
    "watch": "sass --watch --load-path=node_modules styles.scss css/styles.css --style=compressed",
    "serve": "lite-server"
  },
  "author": "Borislav Branimirov",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "sass": "^1.63.6"
  },
  "dependencies": {
    "vanilla-framework": "^4.0.0"
  }
}
